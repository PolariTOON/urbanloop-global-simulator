def parcours(table, to_cover):
    table_temp = table
    while to_cover != {}:
        #for l, loop in table :
        print(loop[2] for l, loop in table)
        chosen = {min(loop[2] for l, loop in table)}
        print(chosen)
    return table_temp
